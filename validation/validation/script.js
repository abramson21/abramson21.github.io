/*register.addEventListener('input', (event) => {
    const nameValue = document.forms.register.elements.name.value;
    const newnameValue = nameValue.substr(0,1);
    const reg = /[А-Я]/g;
    const str = newnameValue.match(reg);
    console.log(str);
    const joinSTR = str.join('');

    if (newnameValue != joinSTR ||  зззз === null){
        console.log('НЕ Чётко');
    }
    else {console.log('чётко')}
    //const reg ='^[а-яА-ЯёЁ\s]'
    //nameValue.match(reg);
    //const name = document.querySelector('');
})
*/

